# add details as appropriate for gogs and gitea instances
# the gogs user should have access to every organisation
# the gitea user should be an administrator

# tokens are created by logging in as the user, browsing
# to Settings -> Applications -> Generate New Token

GOGS_HOST=gogs.hostname.domain
GOGS_USERNAME=
GOGS_TOKEN=

GITEA_HOST=gitea.hostname.domain
GITEA_USERNAME=
GITEA_TOKEN=
